working on:
- restructure all conversations using the message flow
done: 
- credit (payment)
- debt (payment)
- paypal
- settings (main menu)
- card
- session
remaining:
- signup
- passive user
- settings submenu inntegration


Coffee Cards:
- Royalty system (adjustable in the settings). either an adjustable amount of free coffee for the creditor or that he doesn't have to contribute to missing coffees. 

Snapshots: 
(- an option to make specific snapshots as permanent, so they are not part of the fixed number of snapshots that are deleted, when more are created.)
(- sub collection snapshots (for example only the inactivity counters of users) and full collection snapshots should cover those sub-collection snapshots as well on restore)

Spreadsheet:
- add a emtpy row in the spreadsheet above the table to make it less cramped
- gsheet update is not carried out anymore reliably after card creation (manual sync works)

- add two-way sync to gsheet, so changes remote can also be propagated to the bot, but only if an admin allows this, especially if something is marked as paid.
- allow searching for changes on the google spreadsheet and propagating them down before updating the spreadsheet. Has to be fast
mongo 
- after a card is completed, in the syncing process to GSHEET, the card is still marked as active there
(- if a card is completed because all coffees were used up it does not update the spreadsheet but earlier when the session was closed, so the card is not marked as completed in google spreadsheet yet)

Credit menu:
- Schredder still appears with 0 outstandign credit, so I think that debts might not be correctly marked as paid at all places


General:
- catch the Ctrl+C in the terminal, to make sure that the bot is closed properly
- unify certain repeating code snippets as functions 
- add an option to message flows, which allow actions on initiation, submit and cancel -> so I can so gsheet synchronizations on sumbit and start backups on initiation save them on submit and delete them on cancel 

Settings:
- pagination for logging modules is not default
- Settings for vanishing messages are currently not consistent between bot restarts. It looks like they are not correctly saved in the database or are overwritten by another place
- make each toggle button the same, so that an activated option has a green dot and an deactivated an red dot on the toggle button (instead of showing how the stat would change if you press the button), this should also include all of the checkmark/cross icons in toggle buttons used somewhere else. This should be completely consistent in the whole message flow.
- make sure that all submenues are correctly implemented as message flow. Currently most of this is still manually in the conversation manager


maybe:
- emulate other users

- reorganize the code, to put general stuff into the services and remvove the telethon specific send message stuff -> should only go via the api, which should be more generic
this should also allow to use the restapi with a webpage instead (except for notifications)

- make settings generate dynamically from the settings entries without having to define each flow. Just a few templates for certain types (true/false toggle), range, a list of possible strings


not working:
- add a reply keyboard and close if pressed and reopen if the coversation finishes. 



# next
- enfore the paypal flow being carried out before the a user can create a new card, in case he doesnt have a paypal link setup yet.


### Google Spredsheet two-way syncing:

Can you help me implement a two-way syncing.
The idea behind this is, that users should be able to make changes in the google spreadsheet (only for the paid amount, all other cells should be protected).
And whenever a sync is triggered, either by action or by a periodic sync, the bot should check for changes in the upstream spreadsheet and propagate thos to the bot.
it is important that this only affects paid amount so far.
Also I need a mechanism, to make sure that changes of the paid amount in the bot are priotised over the changes in the spreadsheet, if they affect the same value.
this means, that for paid amounts, the actual changed values should be forwarded to the syncing algorithm, so it does not overwrite them
